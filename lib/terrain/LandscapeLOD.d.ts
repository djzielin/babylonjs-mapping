/** Shared Tokyo–Fuji landscape profile, scaled to a source tile's world width. */
export declare function landscapeTerrainLOD(meshPrecision: number, tileWorldWidth: number): {
    precisions: number[];
    distances: number[];
};
